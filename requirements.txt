# Core dependencies for PyThermalCamera
# Topdon TC001 Thermal Camera Software

# Web framework (for FastAPI web interface)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Computer vision and image processing
opencv-python>=4.8.0
numpy>=1.24.0

# Note: On Raspberry Pi, you may need to install opencv-python via apt instead:
# sudo apt-get install python3-opencv
